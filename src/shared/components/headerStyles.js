const styles = ({
  position: {
    gridArea: '1 / 1 / 1 / 4',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-end',
    padding: '2em',
    backgroundColor: '#fff'
  }
})

export default styles